# ── Local development ────────────────────────────────────────────
DATABASE_URI=file:./effat.db
PAYLOAD_SECRET=            # openssl rand -hex 24
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_WHATSAPP=201060094777

# ── Hosted (Vercel) — set these in the Vercel project, not here ──
# DATABASE_URI=postgresql://...          Neon (Vercel Marketplace → Neon, free)
# BLOB_READ_WRITE_TOKEN=vercel_blob_...  Vercel Storage → Blob (free)
# NEXT_PUBLIC_SITE_URL=https://<project>.vercel.app
